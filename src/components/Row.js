// import { useState, useEffect } from "react";

// const Row = ({ content, excludedWords }) => {
//   const [num, setNum] = useState(0);

//   useEffect(() => {
//     const n = content.split(" ").filter((x) => x === category).length;
//     setNum(n);
//   }, [category, content]);

//   console.log(num);
//   return (

//       <section>
//         <p>{`Words used: ${content && content.split(" ").length}`}</p>
//         <p>{`No of times used: ${num}`}</p>
//       </section>
   
//   );
// };

// export default Row;
